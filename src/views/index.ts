import { page as main } from './main';
import { page as recursion } from './recursion';
import { page as debounce } from './debounce';
import { page as sort } from './sort';

export default {
  main,
  recursion,
  debounce,
  sort,
};
